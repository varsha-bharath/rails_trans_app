<h3>Listing Vehicles </h3>

 <div class="table-responsive">          
  <table class="table" border="1">
  	<thead>
		<tr>
			<th>VehicleName</th>
			<th>RegistrationNumber</th>
			<th>Actions </th>
			
		</tr>
	</thead>
		<tbody>
			<tr>
				<% for vehicle in @vehicles %>
					<td><%= vehicle.name %></td>
					<td><%= vehicle.reg_no %></td>	
					<td><%= link_to "Show",vehicle_path(vehicle.id) %> |
						<%= link_to "Edit",edit_vehicle_path(vehicle.id) %> |
						<%= link_to "Delete",vehicle_path(vehicle.id), method: :delete,data:{confirm:"This vehicle is associated with different routes and drivers,are you sure"}%>
			</td>
		
			</tr>
		</tbody>
<% end %>
</table>
</div>
<br/>

<%= link_to "Add New Vehicle", new_vehicle_path %><br/><br/>

<h3>Listing Drivers</h3><br/>
<div class="table-responsive">          
  <table class="table" border="1">
 	<thead>
		<tr>
			<th>#</th>
			<th>Name</th>
			<th>Mobile No</th>
			<th>Emergency Contact</th>
			<th>Action</th>
        </tr>
	</thead>

    <tbody>
    	<% @drivers.each_with_index do |driver,index| %>
          <tr>
          	    <td><%= index + 1 %></td>
          	    <td><%= driver.name %></td>
          	    <td><%= driver.mobile_no %></td>
          	    <td><%= driver.emergency_contact %></td>
          	    <td><%= link_to "Show", driver_path(driver.id) %> |
          	    	<%= link_to "Edit", edit_driver_path(driver.id) %> |
          	    	<%= link_to "Delete", driver_path(driver.id), method: :delete, data: {confirm: "Are you sure?"} %></td>

          </tr>

          <% end %>

    </tbody>

	</table><br/>
</div>
<%= link_to "Add New Driver" , new_driver_path %><br/><br/>

<h3>Listing Clients</h3>

<br/>
<div class="table-responsive">          
  <table class="table" border="1">
 	<thead>
           <tr>
           	   <th> </th>
           	   <th>Name</th>
           	   <th>Email</th>
           	   <th>Phone</th>
           	   <th>Action</th>
           </tr>
      </thead>

      <tbody>
      	   <% @clients.each_with_index do |client,index| %>
                   <tr>
                        <td><%= index + 1 %></td>
                        <td><%= client.name %></td>
                        <td><%= client.email %></td>
                        <td><%= client.phone %></td>
                        <td><%= link_to "Show", client_path(client.id) %> |
          	    	        <%= link_to "Edit", edit_client_path(client.id) %> |
          	    	        <%= link_to "Delete", client_path(client.id), method: :delete, data: {confirm: "Are you sure?"} %></td>  
                   </tr>
      	   <% end %>
      </tbody>
</table><br/>
</div>

<%= link_to "Add New Client " , new_client_path %><br/><br/>


<h3>Listing Routes</h3>

<br/>
<div class="table-responsive">          
  <table class="table" border="1">
   <thead>
	  <tr> 
            <th>   </th>
            <th>Name</th>
            <th>Action</th>
	  </tr>
  </thead>

  <tbody>
          <% @routes.each_with_index do |route,index| %>
           <tr>
               <td><%= index + 1 %></td>
               <td><%= route.name %></td>
               <td><%= link_to "Show", route_path(route.id) %> |
          	    	<%= link_to "Edit", edit_route_path(route.id) %> |
          	    	<%= link_to "Delete", route_path(route.id), method: :delete, data: {confirm: "Are you sure?"} %></td>
           </tr> 
           <% end %>
  </tbody>

</table><br/>

</div>
<%#= link_to "Add New Location " ,new_location_path %>
<%= link_to "Add New Route " , new_route_path %><br/><br/>

<h3>Listing Locations</h3>

<br/>
<div class="table-responsive">          
  <table class="table" border="1">
   <thead>
    <tr> 
            <th>   </th>
            <th>Name</th>
            <th>Action</th>
    </tr>
  </thead>

  <tbody>
          <% @locations.each_with_index do |location,index| %>
           <tr>
               <td><%= index + 1 %></td>
               <td><%= location.name %></td>
                <td><%= link_to "Show", location_path(location.id) %> |
                  <%= link_to "Edit", edit_location_path(location.id) %> |
                  <%= link_to "Delete", location_path(location.id), method: :delete, data: {confirm: "Are you sure?"} %></td>
           </tr> 
           <% end %>
  </tbody>

</table><br/>

</div>
<%= link_to "Add New Location " ,new_location_path %><br/><br/>
